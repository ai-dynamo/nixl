<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>${statusIcon} NIXL ${BUILD_TARGET} Build</title>
</head>
<body style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; padding: 20px; background-color: transparent;">
    <div style="max-width: 600px; margin: 0 auto; background-color: #ffffff; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.15); border: 1px solid #e1dfdd;">

        <!-- Header -->
        <div style="background-color: ${jobStatus == 'SUCCESS' ? '#107c10' : '#d13438'}; color: white; padding: 20px; text-align: center;">
            <h1 style="margin: 0; font-size: 24px;">${statusIcon} NIXL Container Build</h1>
            <p style="margin: 5px 0 0 0; font-size: 16px;">Build #${BUILD_NUMBER} - ${jobStatus}</p>
        </div>

        <!-- Build Info -->
        <div style="padding: 20px;">
            <table style="width: 100%; border-collapse: collapse; margin-bottom: 20px;">
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">Status:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; color: ${jobStatus == 'SUCCESS' ? '#107c10' : '#d13438'}; font-weight: 600;">${jobStatus}</td>
                </tr>
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">Started by:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd;">${userName}</td>
                </tr>
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">Build Target:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">${BUILD_TARGET}</td>
                </tr>
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">NIXL Version:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-family: 'Courier New', monospace;">${nixlVersion}</td>
                </tr>
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">UCX Version:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd;">${UCX_VERSION == cleanUcx ? "<strong>" + UCX_VERSION + "</strong>" : "<strong>" + UCX_VERSION + "</strong> → <code>" + cleanUcx + "</code>"}</td>
                </tr>
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">Base Image:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-family: 'Courier New', monospace; font-size: 12px;">${BASE_IMAGE}:${BASE_IMAGE_TAG}</td>
                </tr>
                <tr>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">Architectures:</td>
                    <td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd;">x86_64, aarch64</td>
                </tr>
                ${UPDATE_LATEST == 'true' ? '<tr><td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; font-weight: 600;">Latest Updated:</td><td style="padding: 8px 12px; border-bottom: 1px solid #e1dfdd; color: #107c10; font-weight: 600;">Yes</td></tr>' : ''}
            </table>

            <!-- Docker Commands -->
            <div style="margin-bottom: 20px;">
                <h3 style="margin: 0 0 10px 0; font-size: 18px;">🐳 Docker Pull Commands</h3>
                <div style="background-color: #f8f8f8; padding: 16px; border-radius: 4px; border-left: 4px solid #0078d4; border: 1px solid #e1dfdd;">
                    <pre style="margin: 0; font-family: 'Courier New', monospace; font-size: 12px; line-height: 1.4; white-space: pre-wrap;">${UPDATE_LATEST == 'true' ? "# Latest tags (updated)\ndocker pull " + REGISTRY_HOSTESS + "/" + REGISTRY_REPO + "/" + BUILD_TARGET + ":" + BASE_IMAGE_TAG + "-x86_64-latest\ndocker pull " + REGISTRY_HOSTESS + "/" + REGISTRY_REPO + "/" + BUILD_TARGET + ":" + BASE_IMAGE_TAG + "-aarch64-latest\n\n" : ""}# Versioned tags
docker pull ${REGISTRY_HOSTESS}/${REGISTRY_REPO}/${BUILD_TARGET}:${tagName.replace('${arch}', 'x86_64')}
docker pull ${REGISTRY_HOSTESS}/${REGISTRY_REPO}/${BUILD_TARGET}:${tagName.replace('${arch}', 'aarch64')}</pre>
                </div>
            </div>

            <!-- Action Buttons -->
            <div style="text-align: center; margin-bottom: 20px;">
                <a href="${BUILD_URL}" style="display: inline-block; padding: 12px 24px; background-color: #0078d4; color: white; text-decoration: none; border-radius: 4px; font-weight: 600;">View Build #${BUILD_NUMBER}</a>
            </div>
        </div>

        <!-- Footer -->
        <div style="background-color: #f8f8f8; padding: 16px; text-align: center; color: #605e5c; font-size: 12px;">
            Automated message from Jenkins job: <strong>${JOB_NAME}</strong>
        </div>
    </div>
</body>
</html>
